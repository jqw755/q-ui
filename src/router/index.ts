import { h } from "vue"
import { createRouter, createWebHashHistory } from "vue-router"

// import Button from "../views/doc/utton/index.vue"

const router = createRouter({
  history: createWebHashHistory(),
  routes: [],
})

export default router
